# FluxCad48

Minimal CAD automation plugin scaffold.

## Development

- Register automation commands with `CadAutomationPlugin.register_command`.
- Execute commands with `CadAutomationPlugin.run_command`.

## Run tests

```bash
python -m unittest discover -v
```
